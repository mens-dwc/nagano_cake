<div class='container'>

    <div class="mt-5">
        <h1>ショッピングカート</h1>

            ここの下空白クリックで飛んでしまう<div><%=link_to "カートを空にする", all_destroy_cart_items_path, method: :delete,"data-confirm" => '買ってください',class:"row justify-content-end"%></div>

                <table class="table table-hover table-bordered col-xs-10" >
                    <thead>
                        <tr class="info">
                            <th class="col-xs-3">商品名</th>
                            <th class="col-xs-2">単価(税込)</th>
                            <th class="col-xs-1">数量</th>
                            <th class="col-xs-1">小計</th>
                            <th class="col-xs-1"></th>
                        </tr>
                    </thead>

                    <tbody>
                        <% @cart_items.each do |cart_item| %>
                        <tr>
                            <td><%= cart_item.item.name %></td>
                            <td><%= cart_item.item.with_tax_price %>円</td>
                            <td><%= cart_item.quantity %>個</td>
                            <td><%= cart_item.subtotal %>円</td>
                            <td><%=link_to "削除する", cart_item_path(cart_item.id), method: :delete %></td>
                        </tr>
                　　　　<% end %>
                        <tr>
                            <td colspan="3"><%=link_to "買い物を続ける",items_path ,class: 'btn btn-sm btn-primary' %></td>
                            <td>合計金額</td>
                            <td><%= @total %>円</td>
                        </tr>
                    </tbody>
                </table>

        <div class="row justify-content-center py-5">
        <div><%=link_to "情報入力に進む",new_order_path,class: 'btn btn-success'%></div>
        </div>

    </div>
</div>
