<table>
	<thead>
		<tr>
			<th>商品検索</th>
			<th colspan="3"></th>
		</tr>
	</thead>
		<tbody>
			<tr>
		    <% @items.each do |item| %>
				<td><% if item.image.attached? %>
        <%= image_tag item.image, size: "50x50" %>
        <% else %>
        <%= image_tag 'no_image', size: "50x50" %>
        <% end %><br>
				<%= item.name %><br>
				<%= item.price %>円<br>
				</td>
			<% end %>
			</tr>
		</tbody>
</table>